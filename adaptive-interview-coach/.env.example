# Local development
DJANGO_SETTINGS_MODULE=config.settings.local
DJANGO_SECRET_KEY=replace-this-with-a-long-random-development-key
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/adaptive_interview_coach
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Production values are configured by the hosting platform.
# Never commit a real secret key or production database URL.
